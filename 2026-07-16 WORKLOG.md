# 2026-07-16 UI 控制台工作紀錄

> 歷史文件：本檔保存 2026-07-16 當時的實作背景，不代表目前功能或檔案結構。現行規格請讀 [`HANDOFF.md`](HANDOFF.md)，後續變更請讀 [`PROGRESS.md`](PROGRESS.md)。

## 工作目標

當時的目標是讓 UI 進度控制台從單純流程展示，延伸到 UI 項目的筆記與工作記錄，資料來源為 `SGF_UI_DataBase`。

## 當次完成內容

### 動態批次篩選

- 依 Google Sheet 當時的優先／批次欄位產生條件。
- 支援數字與 `P1`～`P100` 類格式。
- 批次數量、篩選結果及詳細內容同步更新。

後續狀態：新版 UI 控制台已不再以此批次篩選作為主要入口，現況以 [`HANDOFF.md`](HANDOFF.md) 為準。

### 頁首與流程視覺化

- UI 頁首時間格式與企劃頁統一。
- 流程階段加入圖示、數量及選取標記。
- 點擊同一階段可解除篩選。
- 控制台切換介面改用黑、米白、灰階。

### 詳細視窗

- 項目名稱作為主要標題。
- 機制、批次及階段顯示為標籤。
- 日期整理為簡潔資訊列。
- 檔案路徑集中顯示於底部。

### 工作記錄寫回

- 詳細視窗加入可編輯工作記錄。
- Google Sheet 新增 `備註` 欄位。
- 前端送出 `updateNote` POST。
- Apps Script 依 `rowNumber` 找到資料列，再依標題尋找 `備註`、`工作記錄` 或 `記錄` 欄位。

後續狀態：UI 主項編輯與獨立討論紀錄已在 2026-08-18 擴充，請參閱 [`PROGRESS.md`](PROGRESS.md)。

## 當次主要修改檔案

- `app_v10.js`：當時仍包含部分 UI 控制台邏輯；後來已拆至 `ui-progress.js`。
- `style.css`：當時包含 UI 流程及詳細視窗樣式；後來主要拆至 `ui-progress.css`。
- `google-apps-script-theme2.js`：新增早期 UI 寫回 API。
- `index.html`：當時仍包含 UI 結構；後來已拆至 `ui-progress.html`。

以上路徑反映 2026-07-16 當時架構，不應據此修改目前程式。

## 當時的部署與測試注意事項

- Apps Script 修改後必須建立新的 Web App 部署版本。
- Google Sheet 欄位名稱必須與 API 支援名稱一致。
- 當時寫回依 `rowNumber` 定位；若 Sheet 重新排序，應重新整理頁面後再寫入。
- API Key 位於前端，只能降低誤用，不能視為安全身份驗證。
- 預定測試包含新增、清空、多行及特殊符號工作記錄。

## 歷史價值

本檔主要用於解釋：

- UI 控制台為何曾使用動態批次與 `rowNumber` 寫回。
- UI 詳細視窗及備註功能的早期設計方向。
- 為何舊提交可能同時修改 `index.html`、`app_v10.js` 和 `style.css`。

若進行新功能開發，請勿在本檔追加內容；請把已完成變更寫入 [`PROGRESS.md`](PROGRESS.md)，規劃內容寫入 [`優化計畫.md`](優化計畫.md)。
