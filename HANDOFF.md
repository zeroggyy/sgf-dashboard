# Codex 跨裝置交接筆記

更新日期：2026-08-12

目前本機專案路徑：`C:\Users\user\Downloads\sgf-dashboard-main`。換裝置後路徑可以不同，但工作目錄必須是包含三個 HTML 的專案根目錄。

## 新工作階段應先做什麼

1. 確認工作目錄為 `sgf-dashboard-main`。
2. 先讀 `README.md`、本檔與 `PROGRESS.md`。
3. 使用 `rg --files` 盤點，不要假設仍是舊的單頁架構。
4. 啟動本地 HTTP server，不要直接開 HTML 檔。
5. 修改前確認使用者指定的是哪個控制台，避免波及其他頁。

## 目前架構狀態

- 多頁拆分已完成，三個控制台皆可獨立開啟並互相切換。
- GitHub Pages 正式站：`https://zerogyy.github.io/sgf-dashboard/`。
- 主題一與三具有 Google Sheet 寫入；主題二目前唯讀。
- 三套 API 完全獨立，更新某一套 Apps Script 不會自動更新其他套。
- 本地資料檔與舊 CSV 已移除，不應重新加入主題二 CSV 串接。

## 最近完成的主題二修改

- 新版欄位已支援：製作批次、機制、項目、項目說明、序號及後續流程欄。
- 網頁不再顯示或篩選「製作批次」。
- 加入期望完成篩選，空白顯示「未定」。
- 交付流程卡可點擊篩選，再點解除。
- 階段顯示名稱已改為「美術製作」與「正式介面」，但 Sheet 欄名仍為「拆圖」與「編輯」。
- 詳細視窗已壓縮：標題位於視窗頂部，D 欄「項目說明」位於內容第一區；英文 `SELECTED ITEM` 與列號不再顯示。
- 詳細視窗已移除製作批次，保留機制、序號、階段、日期、資料完整度與三個可複製路徑。
- 前端 API 加入 20 秒逾時、兩次重試及 localStorage 上次成功資料備援。
- `google-apps-script-theme2.js` 加入 60 秒 Script Cache；此修改必須重新部署 Apps Script 才會上線。
- API 實測曾出現約 4～22 秒回應與偶發 404，所以容錯是必要功能。

## 目前下一項需求（尚未實作）

使用者希望在主題二詳細介面加入具名討論紀錄，典型內容為「誰留下了一段訊息或反饋」。

建議設計：

- 新增 Google Sheet 分頁 `UI_討論紀錄`，不要把多筆留言塞進主表。
- 最小欄位：`記錄ID`、`項目ID`、`項目序號`、`留言人`、`訊息內容`、`建立時間`。
- 可選欄位：留言類型、處理狀態、更新時間。
- 串接鍵優先使用永久不變的 `項目ID`；目前前端 `id` 是由機制、名稱與陣列索引組成，不適合當長期資料鍵。
- 因此實作前最好先在 `SGF_UI_DataBase` 新增固定 `項目ID` 欄，既有資料一次補值，之後不因名稱或排序改變。
- 詳細視窗預設顯示最新 3 筆，超過時提供展開全部；輸入留言人與訊息後以單次 POST 新增。
- Apps Script 需新增 GET 討論與 POST 新增討論。為避免首頁變慢，建議開啟詳細項目時才按項目 ID 定向讀取討論。
- 寫入討論後應清除或繞過主題二的相關快取。

## 已知風險與待確認

1. `ui-progress.js` 的主題二日曆按鈕目前呼叫 `scheduleBtn?.click()`，但該檔案內沒有宣告 `scheduleBtn`。點擊可能產生 `ReferenceError`；主題二日曆功能需要另行修正或正式停用。
2. 主題一 GET 沒有明確逾時、重試與離線備援；若 Apps Script 慢，主題一仍可能直接失敗並打開 API 設定視窗。
3. 主題二本地 Apps Script 檔的 `SPREADSHEET_ID`、`API_KEY` 是占位內容。部署時不可直接覆蓋正式設定而忘記填回。
4. API Key 位於公開前端，不能視為真正機密；它只能降低誤呼叫，不能阻止查看原始碼的人取得。
5. `.git` 目錄存在，但目前環境曾回報不是 Git repository；換裝置後請先執行 `git status` 確認 clone／工作樹狀態，再提交。

## 建議回歸測試

### 共用

- 三頁由控制台抽屜完整切換一圈。
- Escape、遮罩點擊與關閉按鈕可正常關閉視窗。
- GitHub Pages 強制重新整理後仍載入最新 `?v=` 資源。

### 主題一

- API 讀取、搜尋與所有篩選。
- 負責人複選時主要＋支援自動選取；單選時回到主要。
- 新建、編輯、寫回、超連結、週歷程折疊。
- 時程 F/H 欄讀寫與排序功能。

### 主題二

- API 成功、重試狀態及斷線備援。
- 機制、期望完成、搜尋、階段點擊／解除與重設。
- 流程卡數量與流程地圖項目數一致。
- 詳細視窗 D 欄項目說明、日期與路徑複製。
- 若實作討論：新增後不整頁刷新也能看到最新留言。

### 主題三

- 摘要、單武器延遲載入、音效／語音切換。
- 武將總覽、角色篩選、狀態篩選與快速批次編輯。
- 單筆狀態與討論寫回、最新三筆／展開全部。
- 角色清單武器適用勾選與同步矩陣。

## 部署交接清單

- 靜態網頁修改：上傳 GitHub，等待 GitHub Pages 完成。
- Apps Script 修改：貼入對應試算表 Apps Script → 儲存 → 部署 → 管理部署作業 → 編輯 → 建立新版本 → 部署。
- 若保留既有部署，`/exec` URL 不變。
- 主題二這次新增的 60 秒 API 快取尚需確認是否已重新部署。
- 完成部署後以正式 GitHub Pages 測試，而不只測本地頁。
# 2026-08-13｜第四頁 Icon 控制台交接

- 新增 `icon-progress.html`、`icon-progress.css`、`icon-progress.js`。
- 新增 `google-apps-script-icon.js`，同時讀取 `SGF_ICON_DataBase` 與 `SGF_ICON_Locale`。
- 主資料寫回 action：`updateIcon`，使用 `Icon ID` 定位。
- 語系寫回 action：`updateIconLocale`，使用 `Icon ID + 語言` 定位。
- 四個 HTML 的控制台抽屜已加入 `theme4`，集中導覽位於 `dashboard-navigation.js`。
- 實際上線前須部署 Icon 專用 Apps Script，並在 `icon-progress.html` 的 API 設定視窗填入 URL／Key。
- 本機測試已涵蓋資料載入、階段統計、分類展開、詳情、主資料寫回、語系編輯、桌機及手機版。

# 2026-08-18｜主題二美術工作視角

- 保留原有快速篩選、交付流程與機制手風琴，不另建重複工作佇列。
- 美術檢視只保留手上工作條件：可開始製作、退回修改、即將到期、已逾期；不再顯示全部項目與等待需求。
- 美術待辦預設只列「可開始製作」項目；切換其他三個條件時改列相應工作，切到整體流程才恢復查看所有項目。
- 2026-08-18 後續排版：美術待辦預設固定選中「可開始製作」，四個條件改為單選；進度檢視切換與內容收進同一個外框，並在「美術手上工作」旁加入問號說明四項規則、日期來源與數量重疊關係。
- 「美術待辦」與「整體流程」已合併為同一個進度檢視切換，預設只顯示美術待辦，避免兩個相近摘要同時佔據頁面。
- 工作提示可與原有機制、期望完成、階段、特殊條件及搜尋組合篩選；再次點擊同一提示可解除，重設篩選會一併清除。
- 項目卡新增「美術下一步」與目標日期；美術待辦依退回、逾期、七日內到期、可開始製作排序。
- 詳細視窗新增醒目的「美術下一步」提示；「資料完整」改成「追蹤資料完整」，避免誤解為需求已可製作。
- 顯示名稱統一為「美術製作」與「正式介面」；Google Sheet 的既有欄名 `美術拆圖完成`、`企劃整合完成` 維持不變，避免破壞 API。
- 期限警示暫以 `企劃整合目標日`／`期望完成` 判斷，且只對美術製作或退回中的項目顯示。若未來新增美術專屬截止日，應改由該欄位判斷。
# 2026-08-18：介面進度日期欄位與編輯按鈕

- 主項編輯中的「企劃開表日」、「美術可用交付日」、「最終確認日」、「退回日期」與「重新確認日期」改用原生日期選擇器，避免輸入任意文字。
- 「企劃整合目標日」為月份資料，改用月份選擇器；儲存時仍轉回 Google Sheet 現行的 `YYYY.MM` 格式，其餘日期轉為 `YYYY.MM.DD`。
- 舊資料若不是有效日期（例如「延後」），編輯畫面會清空日期欄並顯示提示，必須重新選擇有效日期後才能寫入。
- 詳細資料與編輯欄位統一命名為「美術可用交付日」，對應 Google Sheet 同名欄位。
- 編輯視窗底部「取消」與「儲存變更」按鈕改為等寬、等高排列；窄螢幕仍維持雙欄。
- 主項編輯視窗不再因點擊背景而關閉；使用右上角關閉、取消或 Escape 離開時，若內容已修改但未儲存，會先詢問是否放棄變更。

# 2026-08-18：介面項目討論紀錄

- Google Sheet 使用獨立分頁 `UI_討論紀錄`，欄位依序為：記錄ID、項目ID、留言人、訊息內容、討論類型、當時階段、建立時間、是否隱藏。
- `google-apps-script-theme2.js` 新增 `addUiDiscussion` POST 動作；GET 資料會回傳未隱藏的討論紀錄。
- 介面詳細視窗預設顯示最新 3 筆，可展開全部；新增討論會獨立寫入，不會儲存或覆蓋主項欄位。
- 討論類型包含一般討論、修改要求、處理回覆、完成確認；留言時會保留當下流程階段快照。
- 部署時須將最新版 `google-apps-script-theme2.js` 全文貼回原本主題二 Apps Script 專案，保留實際 `SPREADSHEET_ID` 與 `API_KEY`，建立新版本並重新部署網頁應用程式。
