# SGF Dashboard 技術交接手冊

更新日期：2026-08-25

本檔記錄目前有效的功能、資料來源、維護邊界、測試與部署方式。專案入口見 [`README.md`](README.md)，歷史變更見 [`PROGRESS.md`](PROGRESS.md)，未來規劃見 [`優化計畫.md`](優化計畫.md)。

## Agent 工作規則

1. 修改前先確認目標控制台及對應的 HTML、JavaScript、CSS、Apps Script。
2. 不把 `20260812 backup` 或 `20260813 backup` 當成正式來源，也不要將舊檔覆蓋目前版本。
3. 保留使用者的既有變更；修改前先檢查工作樹與相關檔案。
4. 頁面功能以目前 JavaScript 和 Google Sheet 欄位為準，不依賴過時文件推測。
5. 修改前端資源後，更新 HTML 引用的 `?v=` 快取版本。
6. 修改 Apps Script 不代表線上 API 已更新，必須另外部署新版本。
7. 不在文件、issue 或新程式碼中新增正式 API Key。現有前端 Key 只能防止誤呼叫，不能視為機密保護。
8. 發布前至少完成語法檢查、本地瀏覽器測試及正式站回歸測試。

## 共用架構

- 系統是四頁式純前端網站，部署於 GitHub Pages。
- `dashboard-navigation.js` 管理四個控制台的跨頁導覽。
- `dashboard-shared.js` 提供 Toast 及共用載入狀態。
- 四套 Google Apps Script API 相互獨立；其中一套部署不會更新其他控制台。
- 各頁具有自己的 API 載入、快取、篩選及狀態推導邏輯，目前尚未抽成統一資料層。
- 正式網站為 <https://zerogyy.github.io/sgf-dashboard/>。

## 控制台現行功能

### 1. 企劃進度控制台

主要檔案：`index.html`、`app_v10.js`、`style.css`、`google-apps-script.js`。

- 顯示任務總數、已結案、未結案及整體完成率。
- 支援搜尋、負責人、主要／支援權限、狀態、週別及特殊條件篩選。
- 支援任務群組展開、折疊、專注及排序。
- 可新增與編輯任務、負責人、支援人員、連結、目標、完成狀態和週進度。
- 編輯抽屜提供上週、當週、下週及較早歷程。
- 專案時程可依年、月、週查看及編輯目標。
- 可透過 Apps Script 排序並重新讀取試算表。

資料來源：

- `Task`：第 1 列為週別，第 2 列為欄名，第 3 列起為任務。
- `時程`：C 欄日期、D 欄星期、F 欄目標、H 欄補充。

維護注意：主題一讀取流程的逾時、重試及離線備援仍比其他頁弱，改善方向見 [`優化計畫.md`](優化計畫.md)。

### 2. UI 進度控制台

主要檔案：`ui-progress.html`、`ui-progress.js`、`ui-progress.css`、`google-apps-script-theme2.js`。

- 支援機制、關鍵字、企劃整合目標日、交付階段及特殊條件篩選。
- 提供美術待辦與整體流程兩種進度視角。
- 美術工作條件包含可開始製作、退回修改、七日內到期及已逾期。
- 流程階段為待企劃需求、待製作人確認需求、待程式功能、待代圖操作確認、待美術拆圖、待企劃整合、待製作人最終確認、退回處理中及已結案。
- `製作人需求確認` 是美術開工的必要關卡；欄位不是 TRUE 時，不會進入美術可開始製作清單。
- 詳細視窗顯示下一步、日期、說明、資料完整度、縮圖與交付路徑。
- 主項可編輯並直接回寫 Google Sheet。
- 討論紀錄支援一般討論、修改要求、處理回覆及完成確認。
- GET 具有逾時、重試與上次成功資料備援；後端具有短期快取。

資料來源：

- `SGF_UI_DataBase`：UI 主資料及流程欄位。
- `UI_討論紀錄`：記錄 ID、項目 ID、留言人、訊息、類型、當時階段、建立時間及隱藏狀態。

欄位注意：畫面顯示名稱與 Sheet 實際欄名不一定相同，例如畫面上的美術製作／正式介面，後端仍可能使用 `美術拆圖完成`／`企劃整合完成`，不可只因畫面改名就修改 Sheet 欄名。

需求確認欄位：`SGF_UI_DataBase` 必須包含核取方塊欄位 `製作人需求確認`。第一次部署此功能前，在 Apps Script 編輯器執行一次 `migrateRequirementApprovalColumn()`；已有任一後續階段完成的舊項目會回填 TRUE，其餘項目保持 FALSE，等待製作人確認。

日期注意：企劃整合目標日使用月份格式 `YYYY.MM`，其餘日期通常使用 `YYYY.MM.DD`。

### 3. 音效／語音進度控制台

主要檔案：`sound-voice-progress.html`、`sound-voice-progress.js`、`sound-voice-progress.css`、`google-apps-script-theme3.js`。

- 首頁載入武器摘要，點選武器後才讀取動作與角色語音明細。
- 狀態為未開始、待製作、已製作、待修改、已確認、不需製作及畫面提示用的無內容。
- 音效與語音分開統計，完成率以已確認項目計算，不需製作不納入需完成數。
- 語音可依角色、狀態及排序方式檢視。
- 單筆編輯可更新狀態及新增具名討論。
- 快速編輯可批次變更音效或角色語音狀態。
- 可編輯武器音效風格並同步角色語音矩陣。

資料來源：

- `確認總表`：武器名稱及音效風格。
- 同名武器分頁：動作編號、指令、音效狀態及調整需求。
- `角色清單`：角色資料及各武器適用勾選。
- `角色語音進度`：武器 × 動作 × 角色語音資料。
- `音效討論紀錄`：音效／語音討論與角色範圍。
- `更新紀錄`：最後更新資訊。

### 4. Icon 進度控制台

主要檔案：`icon-progress.html`、`icon-progress.js`、`icon-progress.css`、`google-apps-script-icon.js`。

- 支援類型、子類型、語系、階段、關鍵字和特殊條件篩選。
- 摘要顯示退回、待美術、待切圖、七日內到期及已逾期工作。
- 可檢查資料待補及缺少預覽圖的項目。
- 詳細視窗顯示流程、使用位置、交付資料和同群組語系版本。
- 主資料可用 `Icon ID` 更新；語系資料使用 `Icon ID + 語言` 定位。
- API URL 與 Key 由設定視窗保存於瀏覽器 localStorage。

資料來源：

- `SGF_ICON_DataBase`：Icon 主資料。
- `SGF_ICON_Locale`：語系及圖字版本資料。

維護注意：Icon 使用獨立 Apps Script 部署，避免与其他控制台的 `doGet`／`doPost` 衝突。

## 狀態與進度判斷注意事項

- 四頁目前尚未使用完全一致的進度模型。
- UI 與 Icon 以第一個未完成流程欄位推導目前階段。
- UI 與 Icon 具有目標日、七日內到期及逾期判斷。
- 企劃頁以週進度與任務完成度為主。
- 音效／語音以已確認率與流程狀態為主，目前缺少一致的期限追蹤。
- 若要新增跨頁統計，不能直接拼接四頁現有百分比；應先依 [`優化計畫.md`](優化計畫.md) 統一分母、日期與狀態規則。

## 快取與資料新鮮度

- UI、音效／語音及 Icon 都有不同程度的前端或 Apps Script 快取與失敗備援。
- 修改資料後應同步清除或繞過相關快取。
- 使用備援資料時，畫面應清楚標示最後更新時間，避免將舊資料誤認為即時資料。
- 修改資料格式時，應評估舊 localStorage 快取是否需要失效。

## 本地回歸測試

### 共用

- 四頁由控制台選單完整切換一圈。
- Escape、遮罩及關閉按鈕能正確關閉對話框。
- 載入中、成功、錯誤及使用快取狀態顯示正確。
- 桌機與窄螢幕沒有主要操作被遮擋。

### 企劃

- API 讀取、搜尋、負責人／權限／狀態／週別篩選。
- 新增、編輯、寫回與重新整理。
- 週歷程、時程 F／H 欄及排序。

### UI

- API 成功、逾時重試及斷線備援。
- 美術待辦／整體流程切換與複合篩選。
- 階段數量、機制清單及詳細項目一致。
- 主項編輯、退回資訊、日期格式、路徑及討論寫回。
- 有未儲存變更時關閉編輯視窗會先警告。

### 音效／語音

- 武器摘要及單武器延遲載入。
- 音效／語音、角色及狀態篩選。
- 單筆與批次狀態寫回。
- 討論、武器風格及角色語音矩陣同步。

### Icon

- API 設定、資料讀取及快取備援。
- 摘要、搜尋、各種篩選與分類展開。
- 詳細資料、主資料寫回及語系版本寫回。
- 目標日、退回、缺資料及缺預覽判斷。

## 部署清單

### 靜態網站

1. 更新對應 HTML、CSS、JavaScript 及必要共用檔案。
2. 提高 HTML 資源引用的 `?v=` 版本。
3. 推送至 GitHub Pages 使用的 branch。
4. 等待部署完成，以 `Ctrl + F5` 強制更新。
5. 在正式網址測試讀取、篩選、編輯及跨頁切換。

### Google Apps Script

1. 將最新版 `google-apps-script*.js` 貼入對應 Apps Script 專案。
2. 保留正式環境的 Spreadsheet ID 與 API Key，不要用範例占位值覆蓋。
3. 儲存後進入「部署 → 管理部署作業 → 編輯」。
4. 建立新版本並部署。
5. 若沿用原部署，通常 `/exec` URL 不變；仍需以正式頁面驗證 GET 與 POST。

## 已知技術風險

- API Key 位於公開前端，無法提供真正的身份驗證。
- 四頁重複實作 API、快取、日期及狀態規則，容易產生行為差異。
- 主題一的失敗備援較弱。
- Icon 的 API 設定依瀏覽器 localStorage 保存，不同裝置可能連到不同資料來源。
- 根目錄存在大型歷史備份，修改時容易誤選檔案；正式版本應以根目錄目前檔案為準。
- 目前缺少自動化測試，重要流程仍仰賴瀏覽器回歸。

詳細改善順序與驗收條件見 [`優化計畫.md`](優化計畫.md)。
