# 主題三進度筆記｜武器音效與角色語音追蹤

更新日期：2026-07-24

## 已完成

- 主題三名稱：`SGF 音效語音進度控制台`。
- 已串接 Google Sheet 與 Apps Script Web App。
- 首頁採摘要載入；點開單一武器後才讀取動作與角色語音明細，避免未來資料量增加時一次下載全部資料。
- 武器卡分別顯示音效、語音的「已確認率」與階段分布。
- 語音明細已改成「動作 × 角色」資料列，可用角色下拉選單與狀態篩選交叉查看。
- 語音編輯可個別更新角色的語音狀態與目前語音。
- 討論紀錄仍使用同一張 `音效討論紀錄` 分頁，但已區分音效／語音與對應角色；音效只看音效與共用紀錄，語音只看該角色與共用紀錄。
- 已加入「同步角色語音項目」按鈕：依武器動作與啟用角色，只新增缺少的組合，不覆寫既有資料。

## 正式狀態流程

```text
未開始 → 待製作 → 已製作 → 已確認
                      ↑
待修改 ────────────────┘

未開始 → 不需製作
```

可用狀態：

1. 未開始：初始狀態，尚未判斷或處理。
2. 待製作：已確定需要製作，等待完成。
3. 已製作：製作完成，等待檢查。
4. 待修改：檢查後打回重修。
5. 已確認：檢查通過。
6. 不需製作：已判定不需要製作。

`無內容` 只屬於網頁提示，不寫入 Google Sheet。

## 資料分頁

- `確認總表`：第 3 列起，A 欄武器名稱、D 欄音效風格。
- 同名武器分頁：第 3 列起，A 動作編號、D 指令、G 音效狀態、H 音效調整需求。
- `角色清單`：角色 ID、角色名稱、啟用；啟用可填 `TRUE`、`啟用`、`是`、`YES` 或 `1`。
- `角色語音進度`：由同步功能建立，記錄武器 × 動作 × 角色的語音狀態、語音編號、備註與更新時間。
- `音效討論紀錄`：武器名稱、動作編號、日期、留言人、討論內容、討論類型、角色 ID、角色名稱。

## 下一次接續前必做

目前 Google Apps Script 的已部署版本尚未包含最後的狀態轉換修正；因此各武器分頁 G 欄的下拉選單仍是舊的四種狀態。

1. 用本機的 `google-apps-script-theme3.js` 完整取代 Apps Script 現有程式碼。
2. 儲存後：部署 → 管理部署作業 → 編輯目前 Web App → 建立新版本 → 部署。
3. 回到網頁並重新整理。
4. 按一次「同步角色語音項目」。

同步後會更新每一把武器分頁的 G 欄，以及 `角色語音進度` 的語音狀態欄：

- `待確認` → `未開始`
- `最終確認` → `已確認`
- 下拉選單更新為：未開始、待製作、已製作、待修改、已確認、不需製作。

即使尚未填寫角色清單，同步仍會先執行上述狀態與下拉選單更新；待填好啟用角色後再次同步，才會新增角色語音資料列。

## 主要檔案

- `app_v10.js`：SGF 企劃進度控制台前端。
- `ui-progress.js`：SGF 介面進度控制台前端。
- `sound-voice-progress.js`：主題三前端、篩選、角色語音明細與編輯。
- `dashboard-shared.js`、`dashboard-navigation.js`：通知與跨頁控制台選單。
- `index.html`：SGF 企劃進度控制台入口。
- `ui-progress.html`：SGF 介面進度控制台獨立入口與版面。
- `sound-voice-progress.html`：SGF 音效語音進度控制台獨立入口與版面。
- `style.css`：三頁共用基礎版面與元件樣式。
- `ui-progress.css`：主題二專屬樣式。
- `sound-voice-progress.css`：主題三專屬樣式。
- `google-apps-script-theme3.js`：主題三 Google Apps Script 後端，需手動貼上並部署。
- `README.md`：完整使用與資料結構說明。

## 2026-07-31 多頁架構進度

- 三個控制台已可透過獨立網址開啟，控制台選單改為頁面導覽。
- `ui-progress.html` 與 `sound-voice-progress.html` 已內含自己的完整 DOM，不再執行時讀取 `index.html`。
- 三頁共用 `style.css` 的基礎樣式；主題二、三專屬樣式已拆至 `ui-progress.css` 與 `sound-voice-progress.css`。JavaScript 也已拆成 `app_v10.js`、`ui-progress.js`、`sound-voice-progress.js`；通知與跨頁導覽使用小型共用模組。
- 靜態結構、重複 ID、JavaScript 語法與真實瀏覽器載入皆已檢查。
- 主題二 Google Sheet 資料可載入；主題三已顯示 21 把武器並連線 Google Sheet。
- 使用者已完成初步 Google Sheet 寫入測試，結果正常。
- 已從 `index.html` 移除主題二、三的舊備援 DOM；企劃頁只保留自身介面與共用控制台選單。
- 已完成 JavaScript 與頁面專屬 CSS 的第一階段拆分，並通過三頁載入、主要版面、說明彈窗與跨頁切換測試。
- 已修正企劃頁與 `dashboard-navigation.js` 的 `DASHBOARD_PAGES` 重複宣告；三頁完整切換循環已重新驗證。
